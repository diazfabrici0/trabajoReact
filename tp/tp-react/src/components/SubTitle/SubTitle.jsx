import { Typography } from "@mui/material";

export const SubTitle = ({ text }) => {
  return (
    <>
      <Typography align="center" variant="h6">
        {text}
      </Typography>
    </>
  );
};
